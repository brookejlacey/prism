{
  "name": "prism-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-VM Privacy-Preserving DeFi Router for Polkadot Hub",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "compile": "turbo run compile",
    "test:e2e": "cd packages/dashboard && pnpm test:e2e"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
